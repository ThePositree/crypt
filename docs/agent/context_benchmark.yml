version: 1
updated: 2026-08-11
questions:
  - id: route_after_agents
    question: Which file should an agent read first after AGENTS.md to route context?
    expected_sources: [docs/agent/context_routes.yml]
    required_terms: [context_routes.yml]
  - id: live_config_truth
    question: What is the current source of truth for the live strategy config?
    expected_sources: [AGENTS.md, docs/state/current.yml]
    required_terms: [EXECUTION_STRATEGY_CONFIG, runtime]
  - id: okx_money_truth
    question: Which exchange source is authoritative for live fills, fees, and account equity?
    expected_sources: [AGENTS.md, docs/state/current.yml]
    required_terms: [OKX, fills, fees]
  - id: benchmark_target_doc
    question: What benchmark doc defines the $10,000 strategy comparison target?
    expected_sources: [docs/strategy_benchmark.md, docs/strategy_benchmark.card.md]
    required_terms: [strategy_benchmark.md, "$10,000"]
  - id: benchmark_gate_policy
    question: Is the benchmark a hard gate that blocks owner-promoted production strategies?
    expected_sources: [AGENTS.md, docs/strategy_benchmark.md]
    required_terms: [not a hard, owner]
  - id: backtester_regression_runbook
    question: Which runbook should be used when the owner asks whether the backtester is broken?
    expected_sources: [AGENTS.md, docs/backtester_regression.md]
    required_terms: [docs/backtester_regression.md]
  - id: phase_c_window
    question: What are the phase-C --load-from, --from, and --to timestamps?
    expected_sources: [docs/backtester_regression.md, docs/state/current.yml]
    required_terms: ["2026-07-13T00:00:00Z", "2026-07-29T12:00:00Z", "2026-08-10T22:00:00Z"]
  - id: phase_c_capital
    question: What phase-C starting capital should a replay use?
    expected_sources: [docs/backtester_regression.md, docs/state/current.yml]
    required_terms: ["83.09804366087424"]
  - id: phase_c_deploy_commit
    question: Which deployed commit starts the phase-C live behavior boundary?
    expected_sources: [docs/state/current.yml, docs/execution/live_backtest_reconciliation_2026-07-28.card.md]
    required_terms: [81a4e01]
  - id: exchange_closed_unknown_scope
    question: Is commit 2704c83 deployed to Railway production, and does it affect the money path?
    expected_sources: [docs/state/current.yml, CHANGELOG.md]
    required_terms: [2704c83, deployed, money]
  - id: phase_c_failure_symptom
    question: What exact symptom makes phase C fail around the 2026-08-03T17:00:00Z signal?
    expected_sources: [docs/backtester_regression.md, docs/backtester_regression.card.md]
    required_terms: [2026-08-04T00:58Z, 2026-08-06]
  - id: completed_work_destination
    question: Where should completed historical work go instead of docs/tasks/IN_PROGRESS.md?
    expected_sources: [AGENTS.md, docs/agent/operating_rules.md]
    required_terms: [CHANGELOG.md, CHANGELOG_ARCHIVE.md]
  - id: task_entry_shape
    question: What must an active/backlog task entry include?
    expected_sources: [docs/agent/operating_rules.md]
    required_terms: [What, Why now, Expected gain, Acceptance, Links]
  - id: adr_trigger
    question: When should an agent write a new ADR?
    expected_sources: [AGENTS.md, docs/agent/operating_rules.md]
    required_terms: [trade-off, append-only]
  - id: missing_candle_behavior
    question: What should happen when required candle data is missing before expensive backtest or live order work?
    expected_sources: [AGENTS.md, docs/agent/operating_rules.md]
    required_terms: [fail fast, backfill, missing]
  - id: uv_cache
    question: What environment variable should agent-run uv commands use for cache writes?
    expected_sources: [AGENTS.md, docs/state/current.yml]
    required_terms: [UV_CACHE_DIR, /tmp/uv-cache]
  - id: long_command_eta
    question: What should an agent do with a long command whose ETA is above three minutes?
    expected_sources: [AGENTS.md, docs/agent/operating_rules.md]
    required_terms: [stop, exact command]
  - id: image_pack_allowed
    question: Which docs are allowed candidates for a text-as-image experiment?
    expected_sources: [docs/agent/ai_context_system.md]
    required_terms: [CHANGELOG_ARCHIVE.md, old ADRs, old reconciliation]
  - id: image_pack_forbidden
    question: Which docs must never be available only as vector/image retrieval?
    expected_sources: [docs/agent/ai_context_system.md]
    required_terms: [AGENTS.md, docs/state/current.yml, docs/agent/context_routes.yml]
  - id: card_disagreement
    question: What should the agent do if a card disagrees with its full source doc?
    expected_sources: [docs/agent/ai_context_system.md]
    required_terms: [trust the full doc, fix the card]
