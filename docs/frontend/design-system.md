# Design System

Status: not established.
Revision: 0
Derived from Design Identity revision: none.
Selected visual reference revision/hash: none.

Define technical visual rules only after Design Identity exists or after an
existing frontend application has clear conventions that can be inferred.

When established, give reusable values, composition rules, responsive behavior,
semantic meaning, and implementation references for:

- typography;
- spacing;
- colors and semantic color usage;
- surfaces;
- borders;
- radii;
- shadows and elevation;
- density;
- iconography;
- motion;
- forms;
- tables;
- charts;
- responsive principles;
- semantic states.

Reuse established values instead of inventing one-off visual values per task.
For D3, link every material visual rule to a selected-reference region and its
Signature Traits Matrix row. Preserve observed hierarchy, density, typography
roles, geometry, surface treatment, color roles, icon/illustration treatment,
and responsive relationship. A permissive token list is not a fidelity
contract when it allows a materially different composition.

For every material rule, record one of:

- existing implementation evidence;
- approved visual/product rationale;
- accessibility or platform constraint.

## Validation

- Viewports checked:
- Immutable selected-reference and rendered-capture paths/hashes:
- Side-by-side, overlay, crop, or difference method used:
- Components/screens sampled:
- Largest remaining visible differences and materiality decision:
- Accessibility checks:
- Known exceptions:
